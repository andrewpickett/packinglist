import React from 'react';

/*
 * Helper element to simply create an "empty" div element...one that has a non-breaking space in order to force height.
 */
export default function EmptyDiv() {
	return <div>&nbsp;</div>;
}
